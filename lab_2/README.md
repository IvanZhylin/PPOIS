# Computer Simulation Project

Комплексный проект на C++, симулирующий работу компьютера с полной архитектурой компонентов.

## 📋 Характеристики проекта

- **50+ классов** - полная иерархия компонентов компьютера
- **150+ полей** - детальное описание состояний
- **100 уникальных методов** - разнообразное поведение компонентов
- **30 ассоциаций** - связи между классами
- **12 персональных исключений** - обработка ошибок
- **SOLID, DRY, KISS принципы** - качественная архитектура

## 🏗️ Структура проекта

```
computer_simulation/
├── include/          # Заголовочные файлы (.h)
│   ├── exceptions/   # Исключения
│   ├── interfaces/   # Интерфейсы и базовые классы
│   ├── cpu/          # CPU компоненты
│   ├── memory/       # Память (RAM)
│   ├── storage/      # Хранилище (HDD/SSD)
│   ├── graphics/     # Графическая подсистема
│   ├── motherboard/  # Материнская плата
│   ├── power/        # Блок питания
│   ├── cooling/      # Система охлаждения
│   ├── network/      # Сетевые компоненты
│   ├── peripherals/  # Периферийные устройства
│   ├── audio/        # Звуковая подсистема
│   ├── case/         # Корпус
│   └── os/           # Операционная система
├── src/              # Файлы реализации (.cpp)
│   └── ...           # Аналогичная структура
├── Makefile          # Файл сборки
├── CMakeLists.txt    # CMake конфигурация
└── README.md         # Документация
```

## 🚀 Компиляция и запуск

### Используя Makefile:

```bash
# Компиляция
make

# Запуск
make run

# Очистка
make clean

# Debug версия
make debug

# Release версия с оптимизацией
make release

# Полная пересборка
make rebuild
```

### Используя CMake:

```bash
# Создание директории сборки
mkdir build && cd build

# Конфигурация
cmake ..

# Компиляция
make

# Запуск
./computer_simulation

# Или одной командой
cmake .. && make && ./computer_simulation
```

### Прямая компиляция (GCC):

```bash
g++ -std=c++14 -Iinclude src/**/*.cpp src/*.cpp -o computer_simulation
./computer_simulation
```

## 📦 Модули проекта

### CPU (Процессор)
- `RegisterFile` - регистры процессора
- `ALU` - арифметико-логическое устройство
- `ControlUnit` - блок управления
- `CPUCore` - ядро процессора
- `CPUCache` - кэш-память (L1/L2/L3)
- `CPU` - главный класс процессора

### Memory (Память)
- `MemoryController` - контроллер памяти
- `MemoryModule` - модуль памяти
- `RAM` - оперативная память

### Storage (Хранилище)
- `StorageController` - контроллер хранилища
- `Partition` - раздел диска
- `HardDrive` - жесткий диск (HDD/SSD)

### Graphics (Графика)
- `VRAM` - видеопамять
- `GPU` - графический процессор
- `GraphicsCard` - видеокарта

### Motherboard (Материнская плата)
- `PCIeSlot` - слот PCIe
- `Chipset` - чипсет
- `BIOS` - базовая система ввода-вывода
- `Motherboard` - материнская плата

### Power (Питание)
- `PowerRail` - линия питания
- `PowerSupply` - блок питания

### Cooling (Охлаждение)
- `Fan` - вентилятор
- `Heatsink` - радиатор
- `CPUCooler` - кулер процессора

### Network (Сеть)
- `NetworkPacket` - сетевой пакет
- `NetworkAdapter` - сетевой адаптер

### Peripherals (Периферия)
- `USBDevice` - USB устройство (базовый класс)
- `Keyboard` - клавиатура
- `Mouse` - мышь
- `Monitor` - монитор

### Audio (Аудио)
- `AudioCodec` - аудио кодек
- `SoundCard` - звуковая карта

### Case (Корпус)
- `CaseFan` - вентилятор корпуса
- `ComputerCase` - корпус компьютера

### OS (Операционная система)
- `Process` - процесс
- `FileSystem` - файловая система
- `OperatingSystem` - операционная система

### Computer
- `Computer` - главный класс, объединяющий все компоненты

## 🎯 Особенности реализации

### SOLID принципы:
- **S**ingle Responsibility - каждый класс отвечает за одну функцию
- **O**pen/Closed - расширяемость через наследование
- **L**iskov Substitution - корректное использование наследования
- **I**nterface Segregation - специализированные интерфейсы
- **D**ependency Inversion - зависимость от абстракций

### Интерфейсы:
- `IPowerable` - управление питанием
- `ICoolable` - охлаждение компонентов
- `IDiagnosable` - диагностика состояния
- `Component` - базовый класс для всех компонентов

### Исключения:
- `CPUOverheatException` - перегрев процессора
- `MemoryOverflowException` - переполнение памяти
- `DiskFullException` - диск заполнен
- `PowerSupplyFailureException` - сбой питания
- `NetworkConnectionException` - сетевая ошибка
- И другие...

## 📚 Примеры использования

```cpp
// Создание компьютера
Computer myPC("Gaming PC");

// Сборка компонентов
myPC.assembleParts();

// Включение
myPC.powerOn();

// Выполнение задач
myPC.performCPUTask();
myPC.allocateMemory(8);
myPC.saveToStorage("data");
myPC.renderGraphics();

// Диагностика
myPC.runDiagnostics();

// Выключение
myPC.powerOff();
```

## 🔧 Требования

- **Компилятор**: GCC 5.0+ или Clang 3.4+ с поддержкой C++14
- **CMake**: версия 3.10+ (опционально)
- **Make**: GNU Make (опционально)
- **ОС**: Linux, macOS, Windows (с MinGW/Cygwin)

## 📄 Лицензия

Учебный проект. Свободное использование в образовательных целях.

## 👨‍💻 Автор

Computer Simulation Project - комплексная демонстрация ООП в C++

---

**Примечание**: Проект создан в образовательных целях для демонстрации принципов объектно-ориентированного программирования, паттернов проектирования и архитектуры сложных систем.